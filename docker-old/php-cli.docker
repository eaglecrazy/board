FROM php:7.4-cli

RUN apt-get update && apt-get install -y libmcrypt-dev default-mysql-client \
    && docker-php-ext-install pdo_mysql

RUN pecl channel-update pecl.php.net && pecl install mcrypt-1.0.3

WORKDIR /var/www
